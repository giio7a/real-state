import {IEnvironment} from './environment.interface';

/**
 * This is not actually used. It is just a placeholder for the actual environment file.
 * Check the angular.json file to see the actual environment file being used.
 */
export const environment: IEnvironment = {
  production: false,
  denueApiToken: 'xxx',
  weatherLayersToken: 'xxx'
}
